fastapi
uvicorn
pydantic[email]
sqlalchemy
mediapipe
opencv-python
openai
python-multipart
python-dotenv
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1
email-validator

# HybrIK dependencies for 3D SMPL pose estimation (most accurate)
torch>=1.9.0
torchvision>=0.10.0
numpy>=1.19.0
scipy>=1.5.0
easydict
scikit-learn
chumpy  # Required for SMPL body model loading

# YOLOv8-pose for fast 2D pose estimation (fallback when HybrIK unavailable)
ultralytics

# Note: HybrIK model files and repo need to be copied from golf-swing-analyzer
# See golf-swing-analyzer/backend/hybrik_repo/ for the full HybrIK repository